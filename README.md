# Trade Finance AI

Deterministic multi-agent pipeline for trade finance document review.

## Current branch work

Agent A handles document intake and context creation.

## Run Agent A

```bash
python -m app.main --bundle data/sample_documents/case_001_clean
```

The run output is written to `runs/run_xxx/`.
